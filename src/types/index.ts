export interface LeetCodeData {
  title: string
  statement: string
  examples: string[]
  constraints: string[]
  language: string
  code: string
}

export interface NudgeResponse {
  nudge: string
} 
